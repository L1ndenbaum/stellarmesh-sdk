# 文档巡检中记录的现有边界

本页记录由源码确认、未在文档整理中修复的行为，不扩展公开保证。

## Python 异步下载取消

`AsyncClient.download_file` 的普通异常路径清理临时文件，但 `asyncio.CancelledError` 不属于该处捕获的 `Exception`。取消发生在临时文件创建后时，不能承诺临时文件一定被删除；目标覆盖仍发生在完整读取之后。调用方应为下载目录制定清理策略。后续若修复，应单独设计取消、后台文件写入与清理顺序的测试，本轮未改变实现。
