# Repository Rules

## Git 与提交

- 每次代码或文档改动完成后必须立即提交 Git，保证每项改动可追溯、可回滚。
- 提交必须保持单一职责，只包含当前已完成事项，不得夹带无关工作区变更。
- 提交前必须确认对应格式化、静态检查和测试全部通过；检查失败时不得提交或宣称已验证。
- 如果检查因外部环境不可用而无法执行，除非用户明确授权，否则不得提交未经验证的改动。

## 格式化、检查与测试

- Go 代码改动后必须运行 `gofmt`、`go vet` 和受影响模块的 `go test`；提交前重新运行只读格式检查。
- Python 代码改动后必须运行 Ruff 自动修复与格式化；提交前重新运行 `ruff check`、`ruff format --check`、mypy 和 pytest。
- Shell 脚本改动后必须运行 `sh -n`；Dockerfile 或容器入口改动必须构建对应镜像。
- 跨语言契约、共享协议或公共基础设施改动必须运行所有受影响语言和服务的测试，不得只运行新增测试。
- 每次提交前必须运行 `git diff --check`；任务交付前必须运行全仓验证并确认工作区干净。

## 架构与文档

- 公共协议与共享清洗约定只能在 `contracts/` 定义一次；所有仍提供该能力的 SDK 与服务必须通过契约测试保持一致。历史 Logging v1/v2 冻结，不恢复已退役的 ingester 或 sink。
- SDK 不得依赖业务项目的配置模块、请求上下文或部署目录；项目相关信息必须通过公开配置或可注入接口提供。
- 长期运行服务不得自动执行数据库迁移，也不得持有管理员或迁移凭据。
- `server-infrastructure` 负责生产资源和迁移执行编排；本仓库只负责自身 Schema、迁移源码和不可变制品。
- 不提交 Compose、真实 `.env`、生产地址、密码、私钥或其他 Secret。
- 所有 Markdown 文档的自然语言内容必须使用中文；服务名、命令、路径、配置键和代码标识可以保留原文。
- 公共接口、配置、行为或操作流程变化时，必须同步更新对应中文文档与导航。
